# Copy to .env.local for local dev, or set in Vercel → Environment Variables
VITE_API_URL=https://thesis-dashboard-api.onrender.com

# Backend only (Render) — optional fundamentals fallback when Yahoo blocks cloud IPs
# ALPHA_VANTAGE_API_KEY=your_key_here
